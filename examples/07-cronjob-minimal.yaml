apiVersion: launcher.gokure.dev/v1alpha1
kind: Application
metadata:
  name: my-cronjob
spec:
  components:
  - name: cleanup
    type: cronjob
    properties:
      image: myregistry/my-cronjob:v1.2.3
      schedule: "0 2 * * *"
