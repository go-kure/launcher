apiVersion: v1
kind: Service
metadata:
  name: ${app_name}
  namespace: ${namespace}
spec:
  type: ${service.type}
  selector:
    app: ${app_name}
  ports:
  - port: ${service.port}
    targetPort: ${service.port}