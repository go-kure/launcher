apiVersion: launcher.gokure.dev/v1alpha1
kind: ClusterProfile
metadata:
  name: test-cluster
spec:
  capabilities:
    certificate:
      rendering:
        issuerRefName: letsencrypt-prod
        issuerRefKind: ClusterIssuer
