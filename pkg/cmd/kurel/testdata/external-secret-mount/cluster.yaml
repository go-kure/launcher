apiVersion: launcher.gokure.dev/v1alpha1
kind: ClusterProfile
metadata:
  name: test-cluster
spec:
  capabilities:
    external-secret:
      rendering:
        secretStoreRef:
          name: vault-cluster-store
          kind: ClusterSecretStore
