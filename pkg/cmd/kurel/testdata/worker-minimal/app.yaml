apiVersion: launcher.gokure.dev/v1alpha1
kind: Application
metadata:
  name: my-worker
  namespace: default
spec:
  components:
    - name: backend
      type: worker
      properties:
        image: ghcr.io/example/worker:v1.0.0
