This fixture requires the branch-only lowering rules in pkg/oam/builtin/lowering
and pkg/oam/builtin/policies, which the production CLI (newBuiltinTransformer,
build.go) never registers. TestFixtures (fixtures_test.go) skips any directory
carrying this marker; TestToyWebApplication_LoweringChain (toy_webapplication_test.go)
drives it directly with a Transformer that adds those rules.
